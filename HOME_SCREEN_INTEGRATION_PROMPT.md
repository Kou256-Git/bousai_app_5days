# 防災アプリ統合実装指示書

あなたは、既存の防災アプリに他の画面を統合する実装担当AIです。以下の仕様を必ず読み、ホーム画面の見た目・操作・状態・ルート契約を壊さずに、既存画面を1つのシステムとして接続してください。

## 1. 最優先の実装方針

- ホーム画面は既存の基準実装です。画面を作り直したり、独自のデザインシステムへ置き換えたりしないでください。
- 共通ヘッダー、ナビゲーション、文字サイズ変更、ログイン状態表示は全画面で同じものを使ってください。
- 既存のFlaskルート、テンプレート継承、セッション名、APIレスポンス形式を優先してください。
- 他の人が作成した画面は、機能を削除せず、共通レイアウトへ接続してください。必要な変更は、リンク・認証・余白・文字サイズ・レスポンシブ対応に限定してください。
- 未ログインの住民がホーム画面と避難所検索を使え、管理者だけが避難所登録と指示・発信ボードを使える構成を維持してください。
- 日本語UI、アクセシビリティ、スマートフォン対応を必須とします。

## 2. 技術構成

- バックエンド: Python Flask 3.0.3
- テンプレート: Jinja2。各画面は `base.html` を `{% extends "base.html" %}` で継承する。
- 静的ファイル: `bousai_app/static/`
- データ: `bousai_app/data/` のJSONファイル
- アプリ起動: `python app.py`、ポートは5000
- デプロイ: `vercel.json` の `@vercel/python` 設定を維持する。
- 外部地図: Leaflet 1.9.4 と OpenStreetMapタイルを使用する。
- JavaScriptは既存テンプレート内のDOM要素IDとイベント契約を壊さない。新規処理は既存IDとの衝突を避ける。

## 3. 共通レイアウトの完全仕様

### 3.1 ページ全体

- HTML言語は `ja`。
- `meta viewport` を設定する。
- ページ背景は白。
- 本文のフォントは日本語ゴシック系で、既存の優先順位は `Yu Gothic`, `Hiragino Kaku Gothic ProN`, `Meiryo`, sans-serif。
- ルートCSS変数を維持する。

```css
:root {
  --font-scale: 1;
  --primary-blue: #d8d8d8;
  --text-blue: #222;
  --button-blue: #dceaf8;
  --line-color: #999;
  --page-background: #fff;
}
```

- 全要素に `box-sizing: border-box` を適用する。
- メインコンテンツはデスクトップで `width: min(100% - 40px, 1280px)`、上下余白は上20px・下24px。
- スマートフォン（幅600px以下）ではメイン幅を `min(100% - 20px, 1280px)`、上余白を10pxにする。

### 3.2 ヘッダー

- 高さの目安は80px以上。左右14px、上下8pxの余白。
- 背景は `#d8d8d8`、2pxの濃い枠線。
- 左側に「防災アプリ」を表示し、クリックで `/` に戻る。
- タイトルは太字ではなく、通常ウェイト。基準サイズは29px。
- 右側に次をこの順番で表示する。
  1. `文字サイズ変更`
  2. `標準` ボタン
  3. `拡大` ボタン
  4. ログイン済みなら `ログアウト` と `ログイン中`
  5. 未ログインなら `ログイン` と `未ログイン`
- ボタンの背景は薄い青 `#dceaf8`、枠線は `#8998a6`、角丸は追加しない。
- 文字サイズボタンは `aria-pressed` を正しく更新する。
- `localStorage` のキー `fontSize` を使い、`standard` は倍率1、`large` は倍率1.2とする。ページ移動後も設定を引き継ぐ。

### 3.3 ナビゲーション

- ヘッダー直下に配置し、最大幅は1280px、左右幅はメインと揃える。
- リンクの表示順と認証条件は次の通り。
  - 常に表示: `避難所検索へ` → `/shelter_search`
  - ログイン時のみ表示: `避難所登録` → `/shelter_register`
  - ログイン時のみ表示: `指示・発信ボード` → `/board`
- 各リンクは薄い青背景、1px枠線、最小高さ37px、左右余白26px。
- 未ログインで保護ページへ直接アクセスした場合はログイン画面へ送り、ログイン後に元のURLへ戻す。
- スマートフォンでは2列グリッド、間隔8pxにする。

## 4. ホーム画面のレイアウト

- URLは `/`。
- デスクトップでは左右2カラム。左カラム約793px、右カラムは残り幅、カラム間は32px。
- スマートフォン（幅600px以下）では1カラム、間隔18px。
- 入力欄・地図・情報欄は枠線で区切るが、カードを過度に丸めたり、影を付けたりしない。
- 見出しの基準サイズは36px相当。文字サイズ拡大時は全体倍率に従う。

## 5. 左カラム: 災害・被害投稿

### 5.1 表示

- 見出しは `災害・被害投稿`。
- コメント入力欄とファイル選択欄を横並びにする。
- コメント欄は高さ232px、内側余白16px、リサイズ不可、プレースホルダーは `コメントを記入してください`。
- コメント欄の基準文字サイズは24px。
- ファイル選択欄は高さ128px。表示文言は初期状態で `ファイルを選択してください`。
- ファイル欄の補助文言は `動画・画像のみ選択可`。
- 対応拡張子は次だけとする。
  - `.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`, `.bmp`, `.heic`
  - `.mp4`, `.mov`, `.avi`, `.webm`, `.mkv`, `.m4v`
- 投稿できるファイルは最大3個。複数選択に対応する。
- 選択済みファイルは番号付きボタンで表示し、ファイル名をクリックするとプレビューを開く。
- ファイル名は長くてもレイアウトからはみ出さず、折り返しまたはスクロールで確認できるようにする。

### 5.2 操作ボタン

- ファイル欄の隣に `カメラ起動` ボタンを置く。カメラアイコンは `static/icons/camera.svg` を使う。
- 下段に左から、ゴミ箱アイコンのクリアボタン、`投稿` ボタン、エラー／成功メッセージ欄を置く。
- クリアボタンはコメント、選択ファイル、プレビュー状態、エラー表示をすべて初期化する。
- クリアボタンには `aria-label="コメントとファイルをクリア"` を付ける。
- 投稿ボタン押下時、サーバーへ `POST /`、フォーム名 `comment` と複数の `attachment` を送る。

### 5.3 クライアント側の状態と検証

- ファイル選択時に拡張子を検査し、許可外なら投稿せずエラー欄に表示する。
- 4個以上選択された場合は、`選択できるファイルは最大3つまでです。` と表示する。
- 許可外拡張子の場合は、許可される拡張子一覧を含むエラーを表示する。
- エラー欄は赤文字・赤枠・薄い赤背景。成功時は緑文字・緑枠・薄い緑背景。
- 送信成功時は `情報提供ありがとうございます。` を表示する。
- ファイルが選択されていない状態で送信された場合は `ファイルをアップロードしてください。` を表示する。
- サーバー側でも必ず同じ上限・拡張子検査を行う。クライアント検証だけで済ませない。
- 保存ファイル名は元ファイル名をそのまま使わず、安全化した名前とUUID等を組み合わせる。
- 投稿記録にはコメント、元ファイル名、保存ファイル名、拡張子、MIMEタイプ、JSTの作成日時を保存する。

### 5.4 カメラ

- `navigator.mediaDevices.getUserMedia` が利用可能なら背面カメラを優先して取得し、カメラダイアログを開く。
- カメラプレビューは動画要素で表示し、`autoplay`, `playsinline`, `muted` を付ける。
- `写真を撮影` はCanvas経由でPNGファイルを生成し、選択ファイルへ追加する。
- `動画を撮影` はMediaRecorderで録画する。録画中はボタンを `動画撮影を停止` に変更し、再押下で停止する。
- MediaRecorder未対応時は `このブラウザは動画撮影に対応していません。` と表示する。
- カメラ権限拒否・API非対応時はファイル選択を開き、`カメラを利用できないため、ファイル選択を開きます。` と案内する。
- ダイアログを閉じるときはMediaStreamの全トラックを停止し、カメラを解放する。

### 5.5 プレビュー

- 画像はimg、動画はvideo controlsで表示する。
- プレビュー表示にはObject URLを使い、閉じたら `URL.revokeObjectURL` で解放する。
- ダイアログの背景は半透明の黒。白背景、濃い枠線、角丸なしの既存トーンを維持する。

## 6. 左カラム: 電話で被害を知らせる

- 見出しは `電話で被害を知らせる`。
- ボタン文言は `電話アプリを起動`。電話アイコンは `static/icons/phone.svg` を使う。
- ボタン横に `TEL：017-000-0000` を表示する。
- 補足文は次を意味・改行を保って表示する。
  - この電話番号は、災害や被害状況を市の防災対策課へ知らせるための専用ダイヤルである。
  - 電話対応はAIが行うので、AIの指示に従って内容を話してもらう。
  - 災害等に関係のない連絡は極力控えてもらう。
- ボタン押下時は確認ダイアログを開き、番号 `017-000-0000` を読み取り専用で表示する。
- ダイアログの `閉じる` で閉じ、`電話アプリを開く` は `tel:0170000000` を開く。
- `<dialog>` 非対応ブラウザでは直接 `tel:0170000000` へ遷移する。

## 7. 右カラム: 災害情報地図

### 7.1 地図

- 地図のDOM IDは `shelterMap`。高さはデスクトップ262px、スマートフォン260px。
- 初期中心座標は `[40.8246, 140.7400]`、ズーム11。この値は既存実装の契約として維持する。
- Leaflet 1.9.4をCDNから読み込み、OpenStreetMapタイルを使う。
- タイル帰属表示 `© OpenStreetMap contributors` を残す。
- 地図上の災害マーカーは専用LayerGroupで管理し、カテゴリ切替時に古いマーカーを消す。

### 7.2 カテゴリ選択

- 地図上部にセレクトボックスと状態表示を置く。
- セレクトのIDは `disasterType`、状態表示のIDは `mapStatus`。
- 選択肢とAPIカテゴリは完全に次の対応にする。
  - `津波` → `tsunami`
  - `河川氾濫` → `flood`（初期選択）
  - `道路冠水` → `road_flood`
  - `土砂崩れ` → `landslide`
  - `積雪` → `snow`
  - `熊などの獣害` → `bear`
- 初回表示時に初期カテゴリを取得し、選択変更時にも取得する。
- 10分ごとに現在選択中のカテゴリを再取得する。
- 取得中は `[カテゴリ]を取得中`、該当ありは `[カテゴリ]情報あり`、該当なしは `[カテゴリ]：発表されていません`、未提供は `[カテゴリ]：未提供`、失敗時は `[カテゴリ]：取得失敗` と表示する。
- 該当情報がある場合、既存実装と同様に中心地点へ赤系の円マーカーを置き、ポップアップにカテゴリ名とAPIメッセージを表示する。

### 7.3 API契約

- `GET /api/disaster_info/<category>` を利用する。
- 許可カテゴリは `tsunami`, `flood`, `road_flood`, `landslide`, `snow`, `bear` の6つだけ。
- 不正カテゴリはJSON `{"error":"Unknown disaster category"}` とHTTP 404。
- 正常レスポンスは少なくとも次のキーを持つ。
  - `category`: カテゴリID
  - `source`: 情報源
  - `items`: 情報配列
  - `available`: 情報取得可否の真偽値
  - `message`: 画面表示用メッセージ
- 取得した `message` と `source` は右下の災害情報欄（DOM ID `weatherContent`）にも表示する。
- リクエスト失敗時は `情報を取得できませんでした。` を表示し、JavaScript例外を画面に露出させない。

## 8. 災害情報・マニュアル欄

- 地図の下に右カラムの情報欄を置く。
- 未ログイン時は災害情報欄だけを1列表示する。
- ログイン時は `災害情報` と `災害マニュアル一覧` を2列表示する。
- 災害情報の見出しは `災害情報`、内容領域の高さは約242px。
- ログイン時の2つ目の見出しは `災害マニュアル一覧`。
- マニュアル欄は既存の他画面・データ実装がある場合、その内容を表示する。未実装なら既存のプレースホルダーの位置とサイズを維持する。
- ホーム画面は住民向け指示の取得変数を壊さない。既存の `resident_notices` を利用する場合は、表示要件を確認してから追加する。

## 9. 既存ルートと認証の統合契約

以下のURLを変更・削除しない。

| URL | 認証 | 役割 |
|---|---|---|
| `/` | 不要 | ホーム、被害投稿、地図、災害情報 |
| `/login` | 不要 | 管理者ログイン。ログイン後に `next` へ戻す |
| `/logout` | 不要 | セッションを消去してホームへ戻す |
| `/shelter_search` | 不要 | 避難所検索 |
| `/all_shelters` | 不要 | 全施設一覧 |
| `/search_results` | 不要 | 条件付き検索結果 |
| `/shelters` | 不要 | 避難所JSON API。`district`で絞り込み |
| `/shelter_register` | 必須 | 避難所登録 |
| `/board` | 必須 | 住民向け指示・発信ボード |
| `/api/weather_warnings` | 不要 | 気象警報・注意報JSON |
| `/api/disaster_info/<category>` | 不要 | ホームのカテゴリ別災害情報 |

- ログイン状態は既存のFlask sessionキー `logged_in` を使う。
- ユーザー名表示が必要な場合は既存の `session['username']` を使う。
- 未ログインユーザーに対して、管理者専用リンクをHTML上で表示しない。
- ただしURL直アクセス時のサーバー側認証も必須で、表示制御だけで保護したことにしない。
- `next` は同一ホストへの安全なURLだけ受け付ける。

## 10. 統合時にしてはいけないこと

- ホーム画面の配色を紫・暗色・強いグラデーション中心へ変更しない。
- 既存のID（`damagePostForm`, `attachment`, `cameraAttachment`, `shelterMap`, `disasterType`, `mapStatus`, `weatherContent` など）を変更しない。
- 投稿のファイル数制限、拡張子制限、サーバー検証を弱めない。
- カメラのMediaStreamを停止せずにダイアログを閉じない。
- API失敗時に画面全体を白紙にしたり、未処理のエラーを表示したりしない。
- 他画面をホーム画面の中へ無理に埋め込まず、共通ナビゲーションから遷移できる独立ページとして統合する。
- 既存データJSONのキー名を独断で変更しない。
- パスワードや秘密情報を新たに画面へ表示しない。既存の開発用認証情報も本番向けに改善できる場合は、画面仕様と混ぜず別変更として扱う。

## 11. 完了条件

次の全項目を確認してから完了と報告する。

1. `/` が未ログインで表示でき、ヘッダー・ナビ・投稿・電話・地図・災害情報が崩れない。
2. 文字サイズの標準／拡大が全画面で動作し、ページ遷移後も保持される。
3. ファイルを最大3個選択でき、画像・動画のプレビューが開閉できる。
4. カメラ対応ブラウザで写真撮影・動画撮影ができ、非対応時はファイル選択へフォールバックする。
5. 空投稿、4個以上、許可外拡張子をクライアントとサーバーの両方で拒否できる。
6. 電話ダイアログから `tel:0170000000` を開ける。
7. 地図の6カテゴリ切替、取得中、該当あり、該当なし、未提供、取得失敗が確認できる。
8. 未ログイン時は管理者専用リンクとマニュアル欄が表示されない。
9. ログイン後に避難所登録と指示・発信ボードへ移動できる。
10. ログアウト後にホームへ戻り、管理者専用リンクが消える。
11. 幅600px以下で1カラム化し、文字・ボタン・入力欄・地図が横にはみ出さない。
12. `python app.py` で起動し、既存のFlaskルートとAPIにHTTPエラーがない。

## 12. 実装後の確認手順

```bash
python app.py
```

ブラウザで `http://127.0.0.1:5000/` を開き、デスクトップ幅とスマートフォン幅の両方で上記完了条件を確認してください。確認結果には、変更したファイル、確認したURL、成功した項目、未確認の項目、既存仕様に起因する制約を明記してください。